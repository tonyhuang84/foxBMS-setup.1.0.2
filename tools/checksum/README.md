# foxBMS Checksum Tool

The foxBMS checksum tool takes an *.hex file as input and calculates the
checksum and writes it back into the *.hex file.

Some output information is stored in the output directory.
